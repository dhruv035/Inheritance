on:
  pull_request:
    branches:
      - main

jobs:
  quick-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print Variables
        run: |
          echo "Repository Event: ${{ github.event }}"
